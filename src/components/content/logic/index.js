import {
        columnasTabla,
        omitirColumnas,
        tablaExpansible,
        ModalEditarFila
} from './crearTabla'

import {
        eliminarMueble,
        getCab,
        getCabinets,
        getVariants,
        handleUpdateCabinet,
        editarVariante,
        eliminarVariante,
        agregarMueble,
        editarMueble,
         agregarVariante
} from './muebles'

import {
        obtenerMuebleIndex,
} from './btnLogic'

export {
        columnasTabla,
        eliminarMueble,
        getCab,
        getCabinets,
        getVariants,
        handleUpdateCabinet,
        omitirColumnas,
        tablaExpansible,
        obtenerMuebleIndex,
        editarVariante,
        eliminarVariante,
        ModalEditarFila,
        agregarMueble,
        editarMueble,
        agregarVariante
}

//Archivo barril --> Folder Lógica